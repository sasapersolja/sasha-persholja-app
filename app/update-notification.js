(()=>{if(!('serviceWorker'in navigator))return;let refreshing=false;function createUpdateBanner(){let banner=document.getElementById('appUpdateNotice');if(banner)return banner;banner=document.createElement('div');banner.id='appUpdateNotice';banner.hidden=true;banner.setAttribute('role','status');banner.setAttribute('aria-live','polite');banner.style.cssText='position:fixed;left:50%;bottom:18px;z-index:110;transform:translateX(-50%);display:flex;align-items:center;gap:12px;max-width:calc(100vw - 28px);padding:10px 14px;border:1px solid #48bde8;background:rgba(0,0,0,.94);color:#fff;font:900 .78rem Arial,Helvetica,sans-serif;letter-spacing:.04em;text-align:center;box-shadow:0 0 18px rgba(72,189,232,.35)';const text=document.createElement('span');text.textContent='New app version available.';const button=document.createElement('button');button.type='button';button.textContent='Refresh';button.style.cssText='min-height:44px;padding:8px 14px;border:1px solid #ff1730;background:#111;color:#fff;font:900 .78rem Arial,Helvetica,sans-serif;text-transform:uppercase;cursor:pointer';banner.append(text,button);document.body.appendChild(banner);return banner}function showUpdate(worker){const banner=createUpdateBanner();const button=banner.querySelector('button');banner.hidden=false;button.onclick=()=>{button.disabled=true;button.textContent='Updating…';worker.postMessage({type:'SKIP_WAITING'})}}window.addEventListener('load',async()=>{try{const registration=await navigator.serviceWorker.register('./service-worker.js');if(registration.waiting&&navigator.serviceWorker.controller)showUpdate(registration.waiting);registration.addEventListener('updatefound',()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener('statechange',()=>{if(worker.state==='installed'&&navigator.serviceWorker.controller)showUpdate(worker)})});navigator.serviceWorker.addEventListener('controllerchange',()=>{if(refreshing)return;refreshing=true;window.location.reload()})}catch(error){}})})();