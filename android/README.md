# Android wrapper

This folder is reserved for the Android package that will wrap the web app after the mobile web version is approved.

The production website repository is intentionally not used here. Android work stays inside this separate app repository.
